/**
 * Options for prefix and suffix decorators.
 */
export type PrefixSuffixOptions = {
  /** Prefix to add to attribute values */
  prefix?: string;
  /** Suffix to add to attribute values */
  suffix?: string;
};

/**
 * Options for index decorators.
 */
export type IndexDecoratorOptions = {
  /** Name of the index */
  indexName: string;
};
